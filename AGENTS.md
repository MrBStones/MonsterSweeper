# Agent Instructions

This workspace uses Bun as the package manager and runtime. Prefer `bun` commands over `npm`, `yarn`, or `pnpm` when running scripts, installing dependencies, or starting the app.

Before running any terminal command, check whether the environment is Windows, macOS, or Linux and choose the appropriate command syntax for that platform.
